{
  "name": "deckdiff",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "check": "pnpm -r check",
    "dev:api": "pnpm --filter @deckdiff/api dev",
    "dev:web": "pnpm --filter @deckdiff/web dev",
    "format": "oxfmt package.json pnpm-workspace.yaml tsconfig.base.json vitest.config.ts apps packages",
    "format:check": "oxfmt --check package.json pnpm-workspace.yaml tsconfig.base.json vitest.config.ts apps packages",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "@typescript/native-preview": "beta",
    "oxfmt": "^0.46.0",
    "vitest": "^3.1.2"
  },
  "packageManager": "pnpm@10.33.0"
}
