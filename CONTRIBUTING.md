# Contributing

Thanks for helping with DeckDiff.

## Setup

```bash
pnpm install
pnpm dev
```

## Before Opening a PR

```bash
pnpm test
pnpm build
```

- Keep changes small.
- Match the existing style.
- Use `pnpm`, not `npm`.
